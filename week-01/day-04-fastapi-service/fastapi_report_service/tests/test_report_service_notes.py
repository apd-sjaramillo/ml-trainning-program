import pytest


@pytest.mark.skip(reason="Optional service or repository test students may add during the Day 4 exercise")
def test_optional_architecture_note() -> None:
    raise AssertionError(
        "Replace this with an optional service or repository test once those layers exist."
    )
